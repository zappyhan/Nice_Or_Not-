# Core data + modelling stack (CPU only, so the image stays small enough to
# push to any managed registry and start quickly in Kubernetes).
numpy>=1.26
pandas>=2.1
scikit-learn>=1.4
scipy>=1.11
joblib>=1.3

# Serving layer
fastapi>=0.110
uvicorn[standard]>=0.29
pydantic>=2.6

# Development / CI
pytest>=8.0
httpx>=0.27          # required by fastapi.testclient
